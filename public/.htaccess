Options -Indexes

<IfModule mod_headers.c>
    Header set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https://res.cloudinary.com https://grainy-gradients.vercel.app; connect-src 'self' https://urbandrop-staging.kantatech.io https://urbanchat.kantatech.io;"
    Header set X-Content-Type-Options "nosniff"
    Header set X-Frame-Options "SAMEORIGIN"
    Header set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
    Header set Access-Control-Allow-Origin "https://urbandrop.io"
    Header always unset Server
    Header always unset X-Powered-By
    Header always unset X-Turbo-Charged-By

</IfModule>

# URL rewriting rules for Vue Router
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /    # Adjust if your app is in a subdirectory

  # Prevent rewriting files or directories
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Rewrite all other requests to index.html
  RewriteRule ^ index.html [L]
</IfModule>
